import React from "react";
const Footer = (props)=>{
    return (
        <footer id={props.id}>
                {props.content}
            {/* <div className="container">
            </div> */}
            
        </footer>
    )
}
export default Footer
